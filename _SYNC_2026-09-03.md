# _progress.md sync — 2026-09-03

Reconciliation pass against the current codebase (branch `story/REQ-1932-Timesheet-CodeApprover-Scene-Visibility-Enhancement`, HEAD `61a21bd07`). This file is a standalone action list of everything the sync changed in `_progress.md` — no guide content or screenshots were touched. Delete this file once the items below have been worked through, or keep it as a dated record; it isn't read by the sync prompt in `_progress.md`.

**Important caveat on freshness:** this branch's `HEAD` is an *ancestor* of the `v2026.08.05` tag that 96 of the 149 `done` rows cite (i.e. behind it), not ahead of it. `origin/develop` is 176 commits further ahead as of today (2026-09-02/03). The diffing below used literal `HEAD` per instruction, which is valid for rows tagged `v2026.08.02`/`v2026.08.04` (real history in between), but for `v2026.08.05`-tagged rows it likely undercounts drift accumulated since Aug 22. One concrete miss already surfaced this way: the new Job booking review step (see below) only showed up because the discovery pass separately checked `origin/develop`. **Recommend a follow-up pass diffing the `v2026.08.05` rows against `origin/develop` instead of this branch's `HEAD`.**

---

## 1. New features found (not previously tracked) — added as `todo` rows

### New section: Access & Visibility
A generic per-record "Access" panel (`AccessController`, route `/access/:id`) used across Jobs/Orders/Tickets/Records/Assets/etc. for visibility, ownership, sharing, and tagging. 11 dedicated views, fully routed, zero prior references in the tracker — the single biggest gap found.

- Viewing a record's Access panel (visibility & ownership) — `AccessController#show/#visibility/#ownership`
- Sharing or assigning a record to specific users — `AccessController#shared_users/#assigned_users`
- Tagging or labelling a record from the Access panel — `AccessController#tags/#labels`

### New section: Custom Fields
End-user data entry into a field group's custom fields on any record (`FieldableFieldGroupsController`). This is the runtime/day-to-day counterpart to the admin-only "Building a field set..." guide already tracked under Settings: Workspace Builder — not the same thing, was uncovered.

- Filling in and editing custom field values on a record — `FieldableFieldGroupsController#show/#edit/#update`

### New section: Settings: Overview
- Finding your way around Settings (landing page) — `SettingsController#index`

### New row under Timesheets
- Using the Timesheets landing page — `TimesheetsController#landing` (found incidentally while diffing the Timesheets section; the landing view itself has had copy/tooltip changes with no tracker row to attach them to)

### Maps-to correction (not a new row)
`JobsController#status` (Jobs section, "Changing a job's status", still `todo`) — a new confirm/review step (`#review_book`/`#book`, new views `review_book.html.erb` + `_review_book_form.html.erb`) was added after v2026.08.05, shown before a status change actually books a job. Appended to the existing row's Maps To rather than creating a duplicate.

### Also fixed in passing
"Logging a break or other shift event" (Timesheets) cited `Timesheets::EventsController`, which doesn't exist — corrected to the real top-level `EventsController`. No behavior change, just an inaccurate cell.

### Flagged, not added (needs a human call)
- `Settings::AttachmentsController` / `Settings::FieldsController` per-attachment PDF-visibility/cache toggles — arguably belongs inside the existing "Building a field set..." admin guide rather than its own row.
- `ClientLeadsController#autocomplete`, `MentionsController#index` — JSON-only autocomplete endpoints, no standalone screen, recommended to leave untracked.

---

## 2. Guides needing a rewrite — status flipped from `done` to `needs update`

| Guide                                                              | Section                | What changed                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------ | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Filtering a list view by a field                                   | List Views & Filtering | Text-field filters gained a new "string_options" multi-select (creatable tags) alongside the operator+term input — show this new value picker.                                                                                                                                                 |
| Managing saved views (admin)                                       | List Views & Filtering | Admin edit-view modal's Delete link now correctly points to the settings-scoped delete route (was pointing at the personal `/views` route); an authorization/policy-scope check was also added to the admin filter action.                                                                     |
| Managing cost rates on a product                                   | Products & Rates       | Cost-rate form now uses distinct cost-specific copy ("Cost book", cost title/subtitle/aside text) instead of reusing sell-rate wording; Cancel on a new cost rate now correctly returns to the Costs tab instead of the Rates tab (previously a bug) — re-screenshot the form and cancel flow. |
| Recording actual product usage against an allocation               | Product Allocations    | New required "Status" field added to the recording form and now shown on the detail view (a `ProductRecording` status dropdown) — capture it in the create/edit flow and detail screenshot.                                                                                                    |
| Viewing and editing an individual timesheet                        | Timesheets             | The Active Duration field on the edit/new form now has a `max_hours: 18` cap enforced by `DurationFormComponent` — mention/validate the 18-hour limit.                                                                                                                                         |
| Approving or denying timesheets for your team (weekly review grid) | Timesheets             | Bulk approve/deny (`bulk_status`) now surfaces a flash error message on failure instead of failing silently — show this error state.                                                                                                                                                           |

All other 143 `done` rows were diffed against their recorded Release SHA and confirmed unchanged or cosmetic-only (no tracker action).

**Files to edit** (confirmed to exist on disk, matched by exact title):

- `Views/Filtering a list view by a field.md`
- `Views/Managing saved views (admin).md`
- `Products & Rates/Managing cost rates on a product.md`
- `Product Allocations/Recording actual product usage against an allocation.md`
- `Timesheets/Viewing and editing an individual timesheet.md`
- `Timesheets/Approving or denying timesheets for your team (weekly review grid).md`

---

## 3. Summary numbers after this sync

- Completed: 143 / 399 (was 149 / 393)
- Needs update: 6
- Views: 7 / 9 (was 9/9)
- Products & Rates: 10 / 11 (was 11/11)
- Product Allocations: 6 / 7 (was 7/7)
- Timesheets: 14 / 17 (was 16/16 — total grew by 1 for the new landing-page row)
- Release breakdown (done rows): v2026.08.02: 23, v2026.08.03: 12, v2026.08.04: 12, v2026.08.05: 96
