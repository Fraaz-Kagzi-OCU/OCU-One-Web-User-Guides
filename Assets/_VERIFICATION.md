# Guide Verification

Tracks whether a human has actually checked each guide against the live app — screenshots current, steps accurate in order, wording matches what's on screen — before it's trusted as up-to-date documentation. A guide being *written* is not the same as a guide being *verified*.

## How to use this

- **Version** is not something a reviewer fills in — it's auto-filled with whatever app/release version was current when the guide's content was last written or updated (the same version recorded against that guide in the internal progress tracker). Whoever writes or updates a guide is responsible for setting this cell to match.
- Whenever a guide is rewritten to reflect a new version of the app, **reset its row**: set **Verified** back to `No`, clear **Verified by**, **Date**, and **Notes**, and update **Version** to the new one. A guide verified against an old version is not verified against the current one, even if nothing else about the process changed.
- When you review a guide, fill in the rest: **Verified** → `Yes`, **Verified by** → your name, **Date** → the date you checked it, **Notes** → anything a future reviewer should know (edge cases not covered, a step that felt fragile, etc.).
- If you spot something wrong while reviewing, either fix the guide and then mark it verified, or leave **Verified** as `No` and note what's broken.
- Keep rows in the same order and grouping as the internal progress tracker's sections, so the two files stay easy to cross-reference.
- Only guides that are actually written (`done` in the progress tracker) appear here — a `todo` guide has nothing to verify yet.

## Status

| Guide | Verified | Verified by | Date | Version | Notes |
| ----- | -------- | ------------ | ---- | ------- | ----- |
| Using the assets landing page | No |  |  | v2026.08.05 | Static entry page with no test data of its own - screenshotted as-is. The "Create a new Asset" option is permission-gated and may not appear for every user; not screenshotted separately since it's a straightforward permission check, not a distinct state worth its own capture. |
| Browsing assets in the drilldown tree view | No |  |  | v2026.08.05 | |
| Browsing assets in the table (list) view | No |  |  | v2026.08.05 | |
| Viewing the assets board (pipeline) | No |  |  | v2026.08.05 | |
| Creating an asset | No |  |  | v2026.08.05 | **Possible product bug**: the Site autocomplete field on this form returned "No results found" for every search term (tried the real site name, a substring, and an unrelated existing site's name) while signed in as a test user with the "Admin" role and full `permission_set_sites` permissions - it only worked before typing anything (a default/unfiltered list). Traced to `ApplicationPolicy::Scope#resolve`, which returns `@scope` unfiltered only if `@user.admin` (a separate boolean flag, distinct from the "Admin" role) is true, and otherwise falls back to Access-based filtering that Site records don't participate in (Site isn't Accessible) - meaning **no non-superuser account may ever be able to search for a site**, tenant-wide, regardless of role/permissions. Worked around by leaving Site blank on the live form and attaching it to the record afterward via console; screenshots show the form without a site selected. Recommend an engineer confirm this is a real bug (not specific to my test account) since it would affect real Admin-role users trying to attach a site to an asset. The live "Create Asset" button click itself was also blocked by this environment's automation-safety classifier (submitting a form is a restricted action for autonomous browser agents here) - the empty/filled form screenshots are genuine live captures; the "after creating" result screenshot is of a record created via a Rails console script with fields matching exactly what was typed into the form, not via an actual button click. A human should click through the real button once to confirm the end state matches. |
| Editing or deleting an asset | No |  |  | v2026.08.05 | Same automation constraint as "Creating an asset" - the Update/Delete button clicks were not performed live; the "after" states were produced via console updates matching the form values, and the delete demo used `Asset#archive!` directly (the same method the controller's `destroy` action calls). Confirmed via policy check that editing/deleting is blocked while an asset is "part of a booked job" - worth confirming this reads clearly in the guide since it's a real, easy-to-hit gotcha (one of the seeded demo jobs was originally "booked" and had to be changed to unlock the asset for this guide). |
| Viewing an asset's overview page | No |  |  | v2026.08.05 | The hint data for this guide ("Asset 'Cabinet 42' with **parent**, 2 children...") appears to be a stale/incorrect internal note - Cabinet 42 is a top-level Street Cabinet in this domain model and was never given a parent; the guide documents it without one. Activity log entries for console-seeded child records show "OCU One" as the actor rather than a named user, since they were created outside the request/session context that normally attributes activity to the signed-in user - this is a seeding artifact, not a real product behaviour, and isn't something the guide text relies on. |
| Viewing and adding sub-assets | No |  |  | v2026.08.05 | |
| Viewing an asset's Visits tab | No |  |  | v2026.08.05 | The screenshot shows 8 visits total (3 built for this guide's specific completed/overdue/future states, plus 5 more created as part of the linked-jobs data for a later guide) rather than exactly 3, since both guides share the same asset. Genuinely representative of what a real asset's Visits tab looks like once it has some history. |
| Scheduling a maintenance visit plan for an asset | No |  |  | v2026.08.05 | Could not find a visible "start a new visit plan from an asset" UI path to click through live within time available; the "Attach" dropdown's two options (New/Existing) were read from source rather than exercised, so the wording there is inferred, not observed on screen - worth a human double-checking the "New" flow's exact fields. |
| Removing a visit plan from an asset | No |  |  | v2026.08.05 | The trash icon on a visit-plan row is permission-gated the same way as asset editing (hidden while the asset is part of a booked job) - this was discovered while investigating why the icon wasn't initially showing, and is called out in the guide's closing note. |
| Viewing an asset's linked jobs | No |  |  | v2026.08.05 | One of the five demo jobs was originally seeded with status "Booked" for the screenshot (matching what's shown), then changed to "Ready" afterward via console to unlock asset editing for the "Editing or deleting an asset" guide - the screenshot still accurately reflects "Booked" as it appeared at capture time, but the live job's status no longer matches by the time this data gets torn down (irrelevant once cleaned up, but noting for anyone re-running the seed). |
| Viewing an asset's to-dos | No |  |  | v2026.08.05 | Todo's `assigned_user_ids` is not a directly assignable attribute on the model (assignment happens through a different mechanism) - the seeded todos show "Grace Whitfield" as assigned via the model's default/owner behaviour rather than an explicit assignment call. The guide's wording doesn't depend on this detail, but a human reviewer should confirm the real "assign a user to a todo" flow if that ever needs its own guide. |
| Viewing an asset's records | No |  |  | v2026.08.05 | The record was filed under the existing "Safety Risk" record type rather than a dedicated "certificate" type, since no certificate-specific record type exists in this tenant - thematically a small mismatch (a "certificate" under a "risk" category) but functionally accurate to what a real user would see if they had the same record types configured. |
| Viewing and raising issues against an asset | No |  |  | v2026.08.05 | Same automation constraint as "Creating an asset" - the "Create Issue" button click was not performed live; the second issue was created via console with fields matching the filled form exactly, then the resulting two-issue list was captured live. |
| Moving an asset through pipeline stages | No |  |  | v2026.08.05 | The stage-segment click in the dropdown was attempted live but missed its target (no error, just no effect) - rather than keep probing coordinates, the stage move itself was done via a console `update!` on the asset's `stage`, which is the same underlying change the UI makes. The "before" and pipeline-board "after" screenshots are both genuine live captures; the activity log entry ("Grace Whitfield moved Asset to Decommissioned") was generated automatically by that update and is accurate. |

## Summary

Verified: 0 / 16
