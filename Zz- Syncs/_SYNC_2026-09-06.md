# _progress.md sync — 2026-09-06

Code-drift reconciliation pass against the current codebase (branch `master`, HEAD `8e1a7f46b`). Per explicit instruction for this run, every release tag was diffed against literal `master` rather than `origin/develop`, even though `master` is not a strict git ancestor of some of the release tags (e.g. `git merge-base --is-ancestor v2026.08.05 HEAD` fails in both directions — the tags sit on a diverged branch topology, most likely from release-branch merges/rebases). This file is a standalone action list of everything the sync changed in `_progress.md` — no guide content or screenshots were touched. Delete this file once the items below have been worked through, or keep it as a dated record; it isn't read by the sync prompt in `_progress.md`.

**Important caveat on false positives from tag/branch divergence:** because several release tags aren't strict ancestors of `master`, a plain `git diff <tag> master -- <path>` often surfaces old commits that already predate the guide's `documented_at` date — they just aren't reachable from the tag due to the diverged topology, not because they're new. For every diff hit this run, I cross-checked the actual introducing commit's date against the row's `documented_at`, and where relevant, spot-checked the current guide content. A large number of hits (the filter multi-select, the admin saved-views Delete-link fix + policy-scope check, the cost-rate form copy, the timesheet `bulk_status` flash message, the 18-hour duration cap) turned out to be **already covered** by the current guide content — these were the same items the 2026-09-03 sync already flagged and fixed, and they were confirmed unchanged/no new drift. Only genuinely new commits (dated after the row's `documented_at`) were treated as real drift.

---

## 1. New features found (not previously tracked)

None. Walked `app/controllers/` (all 327 controller files) cross-referenced against every "Maps to" cell in `_progress.md`, plus `config/routes.rb`'s diff since `v2026.08.02`. Findings:

- Two new rule-action controllers exist (`Settings::Rules::Actions::SetRatebookActionsController`, `SetCostbookActionsController`, added 2026-08-10/12, routes added same commit) — already covered by the existing wildcard row "Common action types reference | Settings::Rules::Actions::* controllers". No new row needed.
- `Home::ActivityController#recent` and `Home::TodayController#today` are just the controller endpoints backing the already-tracked "Reviewing the Recent Activity widget" (cites `Home::Activity::RecentService`) and "Reading the Today snapshot widget" (cites `Home::Today::TodayService`) rows — a Maps-to naming mismatch (service vs. controller), not a new feature.
- Every other "no reference found" hit from the sweep was a concern/mixin (`concerns/jobstra/*`), the mobile API namespace (`mobile_api/*`), gateway/integration webhook controllers (`gateway/*`), or `application_controller`/`health_check_controller` — all explicitly out of scope per the sync instructions.

### Flagged, not added (needs a human call — same as the 2026-09-03 sync's precedent)
- `FroalaController` — generic rich-text-editor image/file/video upload+delete backend (used wherever a Froala editor is embedded, e.g. Hub page/article bodies). No standalone screen of its own; recommend leaving untracked or folding a one-line mention into whichever Hub content-editing guide is written.
- `ClientLeadsController#autocomplete`, `MentionsController#index` — still JSON-only autocomplete endpoints, no standalone screen. Re-confirmed, still recommend leaving untracked.
- `Settings::FieldsController` — still arguably belongs inside the existing "Building a field set..." admin guide rather than its own row. Re-confirmed, not added.

---

## 2. Guides needing a rewrite — status flipped from `done` to `needs update`

| Guide | Section | Release (SHA baseline) | What changed |
| ----- | ------- | ------- | ------------ |
| Switching between saved views on a list screen | List Views & Filtering (Views) | v2026.08.02 | Commit `8f3145945` (2026-08-17, one day after this guide's 2026-08-16 write date) fixed `ViewsController#show`'s allocation-type switch statement: a duplicate `when View.allocation_types[:tasks]` case meant selecting a Tasks-type saved view used to fall through to `redirect_back_top` (stay wherever you were); it now correctly redirects to the tasks list. The matching tab-link URL builder in `scenes/view_component` was fixed the same day. Narrow case (only affects Tasks-type views), but it's a real behavior change a user could hit. |
| Allocating products/materials to a job, project, estimate, or variation | Product Allocations | v2026.08.03 | Commit `575f8a8ff` (2026-09-01, well after the 2026-08-13 write date) changed `product_allocations/index.html.erb`: the "Add Product" dropdown is now hidden entirely (not shown in a disabled state) when the allocatable is a `Task`. |
| Editing or removing a product allocation | Product Allocations | v2026.08.03 | PRO-4515 (merged 2026-08-14, one day after the 2026-08-13 write date) added a Status badge column to the Product Recordings history table rendered on `ProductAllocationsController#show`, and changed the User/Date columns to show `work_completed_by`/`completed_at` instead of the original creator/`created_at`. |

All other 160 `done` rows were diffed against their recorded Release SHA (or the newer SHA in a "vX (from vY)" cell) and confirmed unchanged, cosmetic-only, or already reflected in the current guide content despite a tag/branch-divergence false positive (see caveat above).

**Files to edit** (confirmed to exist on disk, matched by exact title):

- `Views/Switching between saved views on a list screen.md`
- `Product Allocations/Allocating products or materials to a job, project, estimate, or variation.md`
- `Product Allocations/Editing or removing a product allocation.md`

`_VERIFICATION.md` rows reset (Verified → No, Verified by/Date cleared, Notes added — Version left untouched per instructions):

- `Views/_VERIFICATION.md` — "Switching between saved views on a list screen" (was `Yes`/Fraaz Kagzi/17/08/2026, now reset)
- `Product Allocations/_VERIFICATION.md` — "Allocating products or materials to a job, project, estimate, or variation" and "Editing or removing a product allocation" (both were already `No` with no verifier/date, so only the Notes cell was filled in)

---

## 3. Summary numbers after this sync

- Completed: 160 / 399 (was 163 / 399)
- Needs update: 3 (was 0)
- Views: 8 / 9 (was 9 / 9)
- Product Allocations: 5 / 7 (was 7 / 7)
- All other topic lines unchanged (no new rows added, no other section affected).
- Release breakdown (done rows): v2026.08.02: 22 (was 23), v2026.08.03: 10 (was 12), v2026.08.04: 32 (unchanged), v2026.08.05: 96 (unchanged)

### Per-batch drift-check coverage (for reference)
- v2026.08.05 (96 rows, checked first): zero file overlap between the 96 rows' mapped paths and the only 8 files that changed between `v2026.08.05` and `master` — all 96 stay `done`, no diffs needed reading.
- v2026.08.04 (32 rows): several diff hits (filter multi-select component, `Settings::ViewsController`/admin-views templates, `RatesController`/cost-rate views, `TimesheetsController`/timesheet views) — all traced to commits predating each row's `documented_at`; zero flips.
- v2026.08.03 (12 rows): 2 flips (above); the sell-rates row's only post-doc-date hit was a content-free `Merge branch 'master' into develop` commit, not real drift.
- v2026.08.02 (23 rows): 1 flip (above); all other rows' mapped files were either untouched or only touched by pre-doc-date commits.
