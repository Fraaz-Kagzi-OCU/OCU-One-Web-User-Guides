# Full sync — 2026-09-14

Ran the complete `_FULL_SYNC.md` chain (file-existence sync → code-drift sync
→ per-topic `_VERIFICATION.md` propagation → root `_VERIFICATION.md` rollup →
README.md check → sanity check), per `/full-sync`.

Baseline: `master` (HEAD `17befcc79`, confirmed identical to `origin/master`).
3 commits ahead of the prior sync's baseline (`f4a8b2bf5`, 2026-09-13): a
schema update, a GET→POST route swap for two export actions, and their
merge commit.

## Step 1 — File-existence sync

Listed every guide `.md` file in all 37 topic folders (excluding
`_VERIFICATION.md`, `_<Topic> - Overview.md`, `attachments/`) and matched
against every `todo`/`done`/`needs update` row in `_progress.md`. Every
folder's file count matched its section's `done + needs update` row count
exactly — **no drift found**: no `todo` row newly has a file, no `done` row
is missing its file, no orphan file without a row. No edits made.

## Step 2 — Code-drift sync

Resolved all 323 `done`/`needs update` rows' Release cells to tags
(`v2026.08.02`–`v2026.09.01`) and diffed each against `master`, grouped by
release to avoid redundant diffing. Cross-checked every candidate hit
against the guide's actual current text (not just the diff) before flipping
anything, since several "hits" turned out to already be documented despite a
stale Release cell (the prior 2026-09-13 sync log independently confirms the
same false positives for Views/ViewsController, Timesheets/bulk_status
+ pagination + start_shift, and Products & Rates/"Managing sell rates").

**1 row flipped `done` → `needs update`:**

| Guide | Section | Release checked | What changed |
| ----- | ------- | ---------------- | ------------ |
| Editing or removing a product allocation | Product Allocations | v2026.08.04 | `app/models/product_allocation.rb` gained a new validation blocking a rate-modifier change once the allocation has invoiced lines, and `app/views/product_allocations/_form.html.erb` now hides the Uplift/Discount modifier section entirely once invoiced (was previously always shown when the form rendered). Guide's own text (still describing an always-present modifier field on Edit) confirmed not to already cover this. |

This is a genuine gap the 2026-09-13 sync missed — that sync's own log
flagged this row as a false positive based on an *unrelated* view change
(`_fields.html.erb`'s recordings-column rename, which does belong to a
different guide) without checking `_form.html.erb` or the model's
validations.

**Confirmed not drifted** (re-verified against actual guide content, not
just diff presence): 4 `ViewsController`/`Settings::ViewsController` rows
(auth-only/formatting changes outside their actions' scope), 2
`Orders::TasksController` "done" rows (changes land on `#new`/`#update`,
covered by the 2 rows already `needs update`), 6 `TimesheetsController` rows
(bulk-status error flash and review-grid pagination limit are already in the
guide's text per its own `_VERIFICATION.md` note; split-time JS change
belongs to the already-current "Rounding or splitting" guide), "Managing
sell rates on a product" (inactive-rates section already documented,
Cancel-href fix invisible to the documented flow), "Allocating products or
materials..." (the widened search-by-reference match is invisible in the
guide's generic "search box" wording, not a contradiction).

**Controller/routes walk:** no genuinely new undocumented end-user-facing
controllers or actions found. Everything unreferenced in `_progress.md`
(`ClientLeadsController#autocomplete`, `MentionsController`,
`HealthCheckController`, `FroalaController`, `MobileApi::*`, `Gateway::*`
integration endpoints, the `Settings::Rules::{Triggers,Conditions,Actions}::*`
and `Settings::*TypePipelinesController` families, `Home::ActivityController`/
`Home::TodayController`) is backend/support-only or already covered under an
existing umbrella row — consistent with the 2026-09-13 sync's same walk.

## Step 3 — Propagated to `Product Allocations/_VERIFICATION.md`

"Editing or removing a product allocation" row: `Verified` was already `No`
with `Verified by`/`Date` already blank (nothing to reset); `Version` left
unchanged at `v2026.08.04 (from v2026.08.03)`; added a `Notes` entry
(matching the Change notes text above). The row's existing `Blocked` cell
(known bug `product-recordings-table-columns-misaligned.md`) was left as-is
— unrelated to this change.

## Step 4 — Root `_VERIFICATION.md` rollup

This was significantly stale independent of Step 2: it only listed 29 of
the 37 topic folders (missing Announcements, Hub - Channels, Hub - Pages,
Hub - Surveys, Jobs, Leads, Permits, Public Share Links entirely), and
Projects/Records were listed with stale totals (33 and 1, vs. the actual
36 and 26). Rebuilt from scratch by reading every topic's `## Status`
table(s) directly (and Settings' own rolled-up Summary line, per its
special-case rule).

- **Topics fully verified: 4 / 29 → 4 / 36** (same 4 topics — Account,
  Notifications, Signing In, Watches — just a corrected denominator)
- **Guides verified: 15 / 232 → 17 / 323** (Views' actual 8/9 and
  Timesheets' actual 1/18 were previously invisible to this rollup; the sum
  now matches `_progress.md`'s Completed + Needs update exactly)

## Step 5 — README.md check

`_progress.md`'s total row count (401) is unchanged this sync, so the
"roughly 399 user-facing workflows" line was left as-is (it's already 2
rows stale from an earlier, unrelated point — out of scope for a same-count
sync, flagging here rather than guessing at when it drifted).

The "N complete areas" list and count needed real fixes, independent of
today's one new drift:

- **Removed** "Settings (63 guides across 18 sub-areas)" from the complete
  list — Settings is actually 63/65 (2 `todo` rows: an attachment-field
  display-options guide in Workspace Builder, a common-condition-types
  reference in Automation) and was never actually complete. Added a
  one-line callout explaining why it's excluded.
- **Added** Products & Rates (11 guides), Timesheets (18 guides), and List
  Views & Filtering (9 guides) — all three were fully rewritten after the
  2026-09-10 code-drift sync flagged them stale, and have been 100% `done`
  in `_progress.md` for a while, but the README's complete-areas list was
  never updated to add them back. Updated the sentence explaining the
  2026-09-10 staleness to reflect that these three are now resolved and
  restored, while Projects and Product Allocations remain incomplete.
- Count word updated "twenty-nine" → "thirty" to match the corrected list
  (27 prior entries − Settings + 3 additions = 30).
- Left the Records/Jobs/Leads/Permits "is now also complete" trailing
  sentences as-is even though Leads/Permits are now redundantly named both
  in the main list and in a trailing sentence — pre-existing prose
  duplication, not a fact error, out of scope for this sync.

## Step 6 — Sanity check

- Per-topic: `done + needs update` in `_progress.md` equals that topic's
  `_VERIFICATION.md` Total for every topic checked (spot-checked all 37,
  full agreement) — confirmed by the root rollup's summed Total (323)
  exactly matching `_progress.md`'s Completed (320) + Needs update (3).
- `Zz - Bugged Guides.md` cross-check: extracted every non-empty `Blocked`
  cell across all 37 `_VERIFICATION.md` files (20 rows, one — "Creating and
  managing rotating shift patterns" — carrying 2 bugs in one cell = 21
  guide/bug pairs) and matched 1:1 against all 21 rows in
  `Zz - Bugged Guides.md`. **No mismatch.**

Nothing failed to reconcile — no stop-and-report needed.

## Summary numbers after this sync

- Completed: 320 / 401 → **320 / 401** (unchanged net — 1 row flipped
  `done` → `needs update`, no new rows)
- Needs update: 2 → **3**
- Product Allocations guides completed: 7 / 7 → **6 / 7**
- Release breakdown (done rows): v2026.08.04 45 → **44** (v2026.08.02: 19,
  v2026.08.03: 9, v2026.08.05: 93, v2026.09.01: 155 — all unchanged)
- Root `_VERIFICATION.md`: Topics fully verified 4/29 → **4/36**; Guides
  verified 15/232 → **17/323**
- README.md: complete-areas count "twenty-nine" → **"thirty"**; Settings
  removed from the list, Products & Rates / Timesheets / List Views &
  Filtering added

No guide content or screenshots were touched.
