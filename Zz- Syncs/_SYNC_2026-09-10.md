# Full sync — 2026-09-10

Ran the complete `_FULL_SYNC.md` chain (Steps 1–6) against `master` (HEAD `ba427f9d4`).
Release tags `v2026.08.03` and `v2026.08.05` are not strict git ancestors of `HEAD`
(nor of `origin/develop`) in either direction — same diverged-topology situation
noted in the 2026-09-06 sync log — but `git diff <tag> HEAD -- <paths>` still
produces valid tree diffs, so drift-checking proceeded against `HEAD` for every
release. No guide content or screenshots were touched.

---

## Step 1 — file-existence sync

Every topic folder's guide `.md` files were matched against `_progress.md`'s
`done` rows (including all 7 `Settings/*/` subfolders). Result: **no changes**.
Every `done` row has a matching file on disk and every file on disk has a
matching `done` row — no orphans in either direction.

## Step 2 — code-drift sync

Diffed every release tag used by a `done` row (`v2026.08.02`, `.03`, `.04`, `.05`)
against `HEAD`, scoped to `app/controllers`, `app/views`, `app/javascript`,
`app/components`, then cross-referenced changed files against each `done` row's
"Maps to" cell (176 rows checked). **9 rows initially looked like drift; 1 was a
false positive** (see note) — net **8 rows flipped `done` → `needs update`**:

| Guide | Section | Baseline | What changed |
| ----- | ------- | -------- | ------------ |
| Recording actual product usage against an allocation | Product Allocations | v2026.08.04 | Recordings table columns changed: "Completed By"/"Completed At" replace "Owner"/"Created At" (`app/views/product_allocations/_fields.html.erb`). |
| Managing a task's product allocations tab | Projects | v2026.08.02 | The "Add Product" control is now hidden entirely for Task allocatables (`app/views/product_allocations/index.html.erb`, `unless @allocatable.is_a? Task`). |
| Viewing a project's overview (main tab) | Projects | v2026.08.05 | New "Original Value" field now shown when the project has one captured (`app/views/orders/_order.html.erb`). |
| Viewing a project's commercial stats dashboard | Projects | v2026.08.03 | New "Original Value" stat tile + column chart shown when present; existing charts shrink 4→3 columns (`app/views/orders/tabs/stats.html.erb`). |
| Managing cost rates on a product | Products & Rates | v2026.08.04 | `RatesController`/`rates/new.html.erb` now set `@cancel_href` to the Costs tab for cost rates — **this looks like a fix for the exact bug tracked in `Zz - Known Bugs/cancelling-a-new-cost-rate-lands-on-the-rates-tab-not-costs.md`** (still listed `Open`/Blocked). Flagged for `/recheck-bugs`, not resolved automatically here. Rate form copy also now says "Cost Book" instead of "Rate Book" for cost rates. |
| Switching between saved views on a list screen | List Views & Filtering (Views) | v2026.08.04 | `ViewsController#show`'s redirect for a Tasks-type view changed from `redirect_back_top` to `redirect_to tasks_path(view_id:)` — re-verify the "My Open Tasks" direct-link scenario the guide's test data describes. |
| Clocking in and starting a shift | Timesheets | v2026.08.02 | Starting a shift while one is already active now surfaces a proper on-form error message (previously silent/unclear); `TimesheetsController#start_shift` now sets `@shift` with a `:base` error. |
| Rounding or splitting a timesheet entry during review | Timesheets | v2026.08.02 | Split-time picker's midnight-clamping JS reworked to wraparound-safe minutes math — re-verify the 22:00–06:00 overnight split screenshot. |

**False positive, left as `done`:** "Viewing and editing an individual timesheet"
(`app/views/timesheets/_form.html.erb` now has `max_hours: 18`) — its own
`_VERIFICATION.md` note already says it was "Rewritten 2026-09-03 to cover the
Active Duration field's 18-hour cap," so the guide already reflects this; the
diff hit was just the release tag predating when the guide was actually written
(same tag/branch-divergence caveat as prior syncs), not real drift.

**New `todo` rows found (lightweight controller/routes walk):** none. Walked
`app/controllers` (327 files) against every "Maps to" reference in `_progress.md`.
The ~155 unmatched names were all either namespace/shorthand text-matching
artifacts (e.g. bare `TaskTypesController` vs. `Settings::TaskTypesController`,
already covered), `MobileApi::*`/`Gateway::*` backend endpoints (out of scope for
this web-guide vault), individual `Settings::Rules::{Actions,Triggers,Conditions}::*`
controllers already covered by the generic "Common trigger/action types reference"
rows, or supporting-only endpoints with no standalone screen (`FroalaController`,
`MentionsController`, `ClientLeadsController#autocomplete`, top-level
`PipelinesController`, `Home::*Controller`) — consistent with the same calls made
in the 2026-09-06 sync.

**Files edited:** `_progress.md` (8 rows' Status/Change notes; added a Change
notes column to the Projects table, which didn't have one; Summary recomputed).

## Step 3 — propagated to per-topic `_VERIFICATION.md`

Reset each of the 8 rows above (`Verified` → No — all were already No; cleared
`Verified by`/`Date` — already blank; `Version` left unchanged; appended a Notes
entry) in: `Product Allocations/_VERIFICATION.md`, `Projects/_VERIFICATION.md`
(×3), `Products & Rates/_VERIFICATION.md`, `Views/_VERIFICATION.md`,
`Timesheets/_VERIFICATION.md` (×2). No per-topic Total/Verified counts changed
(these rows already existed; only their Notes changed).

## Step 4 — root `_VERIFICATION.md` rebuild

Recounted every topic's `_VERIFICATION.md` directly (Settings via its own
Summary line, already accurate at 0/10, no rebuild needed). The root rollup was
already stale independent of today's drift find: it was missing **Contacts &
Addresses** and **Platform Administration** entirely, and had wrong totals for
**Assignments** (said 0/1, actually 0/3), **Timesheets** (said 0/17, actually
0/18), and **Views** (said 6/9, actually 7/9 — a review had been recorded that
never made it into the root file). All fixed.

- Topics fully verified: 4 / 26 → **4 / 28**
- Guides verified: 14 / 169 → **15 / 176**

## Step 5 — README.md

A mechanical recount of every table row in `_progress.md` (per Step 1's own
definition of `Y`) turned up **399, not the 403 previously stated** — a
pre-existing bookkeeping error unrelated to today's drift find. Corrected
`_progress.md`'s Summary denominator and README's "roughly 403 user-facing
workflows" line to 399.

Dropped **Projects, Product Allocations, Products & Rates, Timesheets, List
Views & Filtering** from the "complete areas" list (each now has at least one
`needs update` row) — complete-area count 25 → **20**, with a note explaining
why those five areas dropped off despite being fully *written*.

## Step 6 — sanity check

- Per-topic `done + needs update` in `_progress.md` vs. `_VERIFICATION.md` Total:
  Product Allocations 7=7, Products & Rates 11=11, Projects 33=33,
  Timesheets 18=18, Views 9=9. ✅ all reconcile.
- Root `_VERIFICATION.md` summed Total (176) = `_progress.md` Completed (168) +
  Needs update (8) = 176. ✅
- `Zz - Bugged Guides.md` cross-check against every topic's Blocked column:
  15 guide/bug pairs on each side, exact match. ✅ No mismatch.

---

## Summary numbers after this sync

- Completed: 176 / 403 → **168 / 399** (denominator corrected, see Step 5)
- Needs update: 0 → **8**
- Product Allocations: 7/7 → **6/7**
- Products & Rates: 11/11 → **10/11**
- Projects: 33/33 → **30/33** (3 rows flipped)
- Timesheets: 18/18 → **16/18**
- Views: 9/9 → **8/9**
- Release breakdown (done rows): v2026.08.02 22→**19**, v2026.08.03 10→**9**,
  v2026.08.04 48→**45**, v2026.08.05 96→**95**
- Root `_VERIFICATION.md`: Topics fully verified 4/26→**4/28**; Guides verified
  14/169→**15/176** (both jumps are mostly the pre-existing staleness fix in
  Step 4, not new verification work).

## Flagged for a human

- **`Managing cost rates on a product`** vs. `Zz - Known Bugs/cancelling-a-new-cost-rate-lands-on-the-rates-tab-not-costs.md`:
  code now looks fixed; run `/recheck-bugs` to confirm live, then `/update-guide`
  if confirmed (would also clear its `Blocked` cell and move it to `Resolved` in
  `Zz - Bugged Guides.md`).
- All 8 `needs update` rows above are otherwise ready for `/update-guide`.
