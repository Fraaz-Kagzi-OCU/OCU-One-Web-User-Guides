# Guide Verification

Tracks whether a human has actually checked each guide against the live app — screenshots current, steps accurate in order, wording matches what's on screen — before it's trusted as up-to-date documentation. A guide being *written* is not the same as a guide being *verified*.

## How to use this

- **Version** is not something a reviewer fills in — it's auto-filled with whatever app/release version was current when the guide's content was last written or updated (the same version recorded against that guide in the internal progress tracker). Whoever writes or updates a guide is responsible for setting this cell to match.
- Whenever a guide is rewritten to reflect a new version of the app, **reset its row**: set **Verified** back to `No`, clear **Verified by**, **Date**, and **Notes**, and update **Version** to the new one. A guide verified against an old version is not verified against the current one, even if nothing else about the process changed.
- When you review a guide, fill in the rest: **Verified** → `Yes`, **Verified by** → your name, **Date** → the date you checked it, **Notes** → anything a future reviewer should know (edge cases not covered, a step that felt fragile, etc.).
- If you spot something wrong while reviewing, either fix the guide and then mark it verified, or leave **Verified** as `No` and note what's broken.
- Keep rows in the same order and grouping as the internal progress tracker's sections, so the two files stay easy to cross-reference.
- Only guides that are actually written (`done` in the progress tracker) appear here — a `todo` guide has nothing to verify yet. This folder covers the entire "Projects" feature area, of which only these 29 guides are written so far (out of 33 total rows in that section) — everything else about creating, editing, and working a project is still `todo`.

## Status

| Guide | Verified | Verified by | Date | Version | Notes | Blocked |
| ----- | -------- | ------------ | ---- | ------- | ----- | ----- |
| Managing a task's product allocations tab | No |  |  | v2026.08.02 | Code drift found 2026-09-10: the "Add Product" control is now hidden entirely for Task allocatables — re-verify whether this tab still supports adding products at all. |  |
| Viewing and managing product allocations on a project | No |  |  | v2026.08.02 |  |  |
| Viewing a project's commercial stats dashboard | No |  |  | v2026.08.03 | Code drift found 2026-09-10: new "Original Value" stat tile and column chart now shown when the project has one captured; other charts shrink from 4 to 3 columns to make room. |  |
| Browsing and filtering the projects list | No |  |  | v2026.08.05 | Seeded under the existing "Install" project type/pipeline rather than a fresh type — a freshly-created type has no PermissionSet rows wired up, making its records invisible to everyone regardless of Access visibility (confirmed directly: a fresh "Civils Installation" type's projects 404'd as "no permission" for their own owner). Also: "Install" has a leftover dev-fixture field set ("TEST - Activity Log Evidence") attached, requiring a "Test Field" value on every project of that type — satisfied with a placeholder "N/A" value, harmless but worth an engineer's eye if that fixture is ever meant to be removed. |  |
| Exploring projects as a relationship graph | No |  |  | v2026.08.05 | **Confirmed product bug, not a data or environment issue**: the Explore view's JS client (`app/javascript/controllers/orders/explore_controller.js`) requests graph data via `GET`, but the server route (`POST /orders/explores/filter`) only accepts `POST` — every request 404s, so the graph canvas never populates for anyone. The guide documents intended behaviour from the code/view template and flags this clearly rather than showing a fabricated working screenshot. | Yes: projects-explore-graph-never-loads.md |
| Browsing projects in drilldown (hierarchy) view | No |  |  | v2026.08.05 | The row's own title link navigates straight to the project's page — the folder *icon* (not the title) is what drills into children. Worth confirming this distinction reads clearly in the guide. |  |
| Viewing and moving projects on the pipeline (kanban) board | No |  |  | v2026.08.05 | Drag-and-drop worked as a genuine live action here — no automation-safety block encountered, unlike some earlier guides this session. Both before/after screenshots are real captures. |  |
| Creating a new project | No |  |  | v2026.08.05 | The hint specified a "Civils Installation" project type — used the existing "Install" type instead (a freshly-created type has no PermissionSet rows wired up, making its records invisible to everyone, a bug already hit and worked around in Batch 1 of this section). No automation-safety block — the "Create Project" click was genuine and live. |  |
| Creating a sub-project (child project) under an existing project | No |  |  | v2026.08.05 | Genuine live click throughout; no adaptation needed. The sub-project correctly inherited the parent's client automatically. |  |
| Viewing a project's overview (main tab) | No |  |  | v2026.08.05 | The hint's "1 SLA in jeopardy" and "3 activity entries" weren't achieved: no RAG/SLA indicator was visibly rendered on this project type's overview page, and only 1 activity entry (creation) appeared even after adding a comment and changing the RAG status via the model directly — comments and RAG changes don't appear to generate a separate Activity feed entry for Orders in this app version. The guide describes the parent/children structure and overview layout genuinely observed, without claiming the SLA/activity-count details that weren't actually visible. Worth a human double-checking whether SLA jeopardy indicators exist elsewhere on this page. Code drift found 2026-09-10: a new "Original Value" field is now shown on this overview when the project has one captured. |  |
| Editing a project's details | No |  |  | v2026.08.05 | Genuine live "Update Project" click, no automation block. The native datetime-local input for "Next action due at" needed careful segment-by-segment typing (day/month/year, then an explicit Right-arrow key press before the time) to avoid the browser's date picker garbling the value — not a guide-relevant detail, just a note for future automation. Confirmed the Project Type field is genuinely locked (shown as plain text, not a dropdown) on the edit form. |  |
| Deleting (archiving) a project | No |  |  | v2026.08.05 | Genuine live delete — the confirmation dialog's "Yes" button was clicked for real, no automation-safety block encountered for this action (unlike Create/Update actions in some earlier guides this session). Confirmed via direct query that the project is archived, not destroyed. |  |
| Managing sub-projects on the Children tab | No |  |  | v2026.08.05 | The hint's "one over budget" indicator wasn't achievable — no visible budget/over-spend flag rendered on the Sub-Projects table in this environment (price columns showed "None" throughout, matching the earlier finding on "Viewing a project's overview" that RAG/budget indicators aren't reliably visible here). The guide describes the table's real columns as observed rather than claiming a budget-status indicator exists. |  |
| Viewing key fields rolled up from jobs and tasks | No |  |  | v2026.08.05 | Required creating a brand-new Field Set/Field Type with `key_type: web` from scratch — no key-web field existed anywhere in this tenant before this guide. A 4th job (created for a different guide, "Boiler Fault Repair") also appears on this tab since it shares the same job type, with no key field value entered — left as-is since it's realistic (not every job has every field filled in) rather than removed for a "cleaner" but less honest screenshot. |  |
| Managing the project plan (project groups / checklist tab) | No |  |  | v2026.08.05 | Genuine live clicks throughout (add group, rename, delete) — no automation-safety block. Did not verify the group-level drag-to-reorder handle actually works (only item-level reordering was tested, see below, and that required a workaround) — the guide mentions the handle exists but doesn't claim to have confirmed dragging a group works. |  |
| Adding a new checklist item (job or record) to a project group | No |  |  | v2026.08.05 | The hint suggested a "new linked Job 'Site Induction - Riverside'" as an alternative to a blank to-do — only the Todo path was exercised live (chosen as the clearer, more universally-relevant demonstration); the Job/Record "create new and attach" options are described from the on-screen panel but not clicked through. Worth a follow-up guide session exercising that path specifically if it's considered important enough to show. |  |
| Attaching an existing job or record to a project group checklist | No |  |  | v2026.08.05 | The search box surfaces jobs tenant-wide, including pre-existing dev fixtures ("Load Test Job 0-6", "TEST - delete me - Fit new boiler") — had to narrow the search term ("Boiler Fault" instead of just "Boiler") to get a results list containing only this guide's own seeded job before screenshotting, per this session's standing rule against showing pre-existing test data. Only the Job path was exercised; Record search/attach wasn't tested. |  |
| Completing, editing, reordering, and removing checklist items | No |  |  | v2026.08.05 | **Reorder step used a workaround**: dragging an item's handle with the browser automation tool only produced a text selection, not an actual drag — after two attempts, the reorder was instead applied via a direct database update (`insert_at`) matching what the drag would do, and the resulting "after" screenshot is a genuine live capture of that new state. Toggle-done, edit, and delete were all genuine live actions with no issues. Worth a human manually testing that dragging actually works in a real browser, since this only confirms automated dragging doesn't register — not that the feature itself is broken. |  |
| Viewing and attaching records (surveys/inspections) to a project | No |  |  | v2026.08.05 | Newly-created records default to `visibility: "private"` on their Access row, which made them invisible to the account viewing the page until fixed — worth a human sanity-checking that new records genuinely default to a sensible visibility for the user who created them, rather than assuming the workaround (setting visibility public at creation) is itself the correct behaviour. Only "Incident Report"/"Safety Risk" record types appeared in the "New" record picker for this project type — the hint's "Pre-Works Survey"/"H&S Inspection" types don't exist yet, so two new record types were created to match the hint and used for the "Existing"-equivalent seeded rows; the live "New" demonstration used the existing "Safety Risk" type instead, since only pre-configured types are offered there. |  |
| Viewing and creating jobs from a project | No |  |  | v2026.08.05 | Genuine live "Create Job" click, no automation block. The Project field on the new-job form auto-fills from the project you started from, which the guide calls out. |  |
| Viewing and filtering tasks on a project | No |  |  | v2026.08.05 | Genuine live filtering — added a Status filter chip, then set its value to "In Progress" via the chip's own picker, which correctly narrowed the list to one task. |  |
| Creating a new task under a project | No |  |  | v2026.08.05 | Genuine live "Create Task" click, no automation block. No "Site Survey" task type existed beforehand — created fresh. The hint's "with product allocation" detail wasn't demonstrated since the project has no products allocated to draw from; the Products tab on a new task is shown empty instead. |  |
| Viewing and working a task's overview tab | No |  |  | v2026.08.05 | Status change (New → In Progress) was a genuine live click, though an accidental double status-change (an earlier click registered asynchronously, landing a second click on the wrong dropdown item once the layout shifted) briefly moved the demo task to "Failed" — caught and corrected via a direct status update before the final screenshot, and the "In Progress" screenshot used is from the first successful live click, not the correction. The hint's "download PDF" detail doesn't apply — a task has no PDF export of its own, unlike a project or job; the guide says so explicitly. |  |
| Editing or deleting a task | No |  |  | v2026.08.05 | Both the rename ("Update Task") and the delete confirmation ("Yes") were genuine live clicks. Deleting archives the task (`lifecycle: "archived"`) rather than hard-deleting it — confirmed directly and called out in the guide. |  |
| Viewing and creating estimates on a project | No |  |  | v2026.08.05 | Genuine live "Create Estimate" click, no automation block. The hint's "£12,400" total wasn't reproduced — an estimate's price comes from products allocated to it, not a directly-editable field, so the guide describes the mechanism instead of a specific figure. |  |
| Viewing and creating invoices on a project | No |  |  | v2026.08.05 | Genuine live "Create Invoice" click, no automation block. Same simplification as estimates: total income amount comes from Lines, not a typed figure, so the hint's exact values (£15,000/£8,200) weren't reproduced. |  |
| Viewing and raising variations on a project | No |  |  | v2026.08.05 | Genuine live "Create Variation" click, no automation block. A newly-raised variation lands in **Draft** status, not the hint's "Pending" — Draft is the model's real default and is what the guide describes; the seeded "pending" example was left as Draft rather than force-set to an artificial status. Value again comes from allocated products, not a typed figure. |  |
| Viewing and attaching permits to a project | No |  |  | v2026.08.05 | Only the tab/detail view and the empty new-permit form were captured live — the full live submission wasn't attempted given the number of required fields (Description, Permit Number, Type, Proposed Start, Estimated End) needed to move a permit out of draft; the seeded example permit was created directly instead. The "10 days" hint was matched (permit's Estimated End is 8 days out from Proposed Start, both within the coming fortnight). |  |
| Linking related projects together | No |  |  | v2026.08.05 | Fully genuine live flow: Attach → Existing → search → select → confirm link, all real clicks, no automation block. The search box correctly showed only the one matching seeded project, no pre-existing fixtures leaked into the results. |  |
| Viewing and managing to-dos on a project | No |  |  | v2026.08.05 | The pre-existing "TEST - Activity Log Evidence" TodoType is the only one wired up with a working PermissionSet in this tenant; rather than let that dev-fixture name leak onto screen, built a new "Field Task" TodoType and hand-wired its `PermissionSets::TodoType` row (ancestry under the tenant's root "Admin" Todos permission set) to match — same class of gap as the OrderType/JobType issue found earlier this session, now also confirmed for TodoType. Checkbox toggle was a genuine live click with no automation block. |  |
| Setting a project's RAG status | No |  |  | v2026.08.05 | The RAG Status control doesn't render on a project's Overview page at all unless its Order Type has `rag_mode` set — "Install" (reused throughout this session) had it unset; enabled it (`rag_mode: "manual"`) since it's a simple display setting, not a permissions concern, and reverted it back to unset in teardown. **The dropdown click itself renders "Red" in the DOM immediately but the change did not persist to the database on the first attempt** (confirmed via direct query, reproduced twice) — no visible error appears, so a user could easily believe the change saved when it hadn't. Applied the same fix as previous automation-block cases (set the real value to match, verified via fresh page load), but this is a genuine gap worth a human re-testing outside this automation harness, since it may or may not reflect real user-facing behaviour. | Yes: rag-status-and-job-unlock-silently-fail-to-save.md |
| Locking and unlocking jobs on a project | No |  |  | v2026.08.05 | The **Lock** action (Unlocked → Locked) persisted correctly and cleanly on a genuine live click, including the auto-unbook side effect (confirmed via toast and via direct query: both jobs' status changed from `booked` to `ready`). The **Unlock** action showed the same non-persisting-click issue as the RAG status change above — the button visibly flipped to "Unlocked" but the database still read `locked_by_user` until fixed the same way (set directly, then reloaded the page fresh to confirm). Worth a human re-testing both the RAG and Unlock actions outside this harness to see if this is specific to the automation environment. | Yes: rag-status-and-job-unlock-silently-fail-to-save.md |
| Downloading or previewing a project PDF | No |  |  | v2026.08.05 | The photo-selection screen ("Select images to show on the PDF") works identically to the one already documented in `Media & Attachments/Choosing which attachments appear on a PDF export.md` — confirmed live, including toggling a photo to "Don't show this time". Clicking **Proceed** (both in PDF mode and Preview mode) fails with the same `Grover::JavaScript::Error: spawn Unknown system error -86` already found and documented in that earlier guide — a local headless-Chrome/PDF-rendering environment issue, not a product bug. The guide describes the selection screen (fully verified live) and doesn't claim to show a rendered PDF, since one couldn't be produced in this environment. |  |

## Summary

Verified: 0 / 33
